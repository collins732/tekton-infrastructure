apiVersion: apps/v1
kind: Deployment
metadata:
  name: tekton-app
spec:
  template:
    spec:
      containers:
        - name: tekton-app
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "500m"
